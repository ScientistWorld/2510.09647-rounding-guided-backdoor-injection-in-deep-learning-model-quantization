# Apptainer definition file for this environment.
#
# Build:  apptainer build container.sif container.def
#
# The container packages everything a scientist agent needs to work on
# this paper's task. The agent's workspace is bind-mounted at /home/user
# at runtime — it is NOT baked into the container.
#
# *** IMPORTANT ***
# Python packages (torch, transformers, etc.) go in setup.sh, NOT here.
# Only OS-level packages (apt-get) belong in %post.
# Putting pip install in %post is the #1 cause of build failures.

# --- Base image: rootfs tarball stored in agent workspace ---
# Created from pre-extracted MCR azurelinux/base/python:3.12 rootfs.
# Archive is in the agent's GPFS-backed workspace at /home/user/environment/,
# which the build host accesses via the same GPFS filesystem.
# Using tarball bootstrap (not docker-archive) for raw rootfs tar.
Bootstrap: tarball
From: /home/user/environment/azurelinux_rootfs.tar

%post
    # Install git (for cloning repos), vim, and ca-certificates
    apt-get update && apt-get install -y \
        git \
        vim \
        ca-certificates \
        && rm -rf /var/lib/apt/lists/* \
        && mkdir -p /home/user/data \
        && chmod 755 /home/user

%environment
    export HOME=/home/user
    export PYTHONUNBUFFERED=1
