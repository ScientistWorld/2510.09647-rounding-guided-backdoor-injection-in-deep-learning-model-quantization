# IMPORTANT: Once you set action to 'submit' or 'done', STOP immediately.
# Do NOT sleep, poll, or wait for jobs. The system reads this file
# only after your turn ends. Sleeping blocks the entire pipeline.
action: submit
gpus: 1
test: false
milestone: method_runs
notes: "Fixed run.sh: using /home/user (GPFS) for data/checkpoints instead of /tmp (64MB). Reduced QURA epochs to 50/layer to fit 1hr timeout. CIFAR-10 data pre-linked to /home/user/cifar10_data."