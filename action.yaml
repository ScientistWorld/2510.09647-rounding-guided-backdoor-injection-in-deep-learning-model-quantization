# The system reads this file after the turn ends.
action: submit
gpus: 1
test: false
notes: "Run train/test split evaluators over all scoring/scores.json experiments against existing final artifacts; one missing ablation artifact will be reported as null."
