# IMPORTANT: Once you set action to 'submit' or 'done', STOP immediately.
# Do NOT sleep, poll, or wait for jobs. The system reads this file
# only after your turn ends. Sleeping blocks the entire pipeline.
action: submit
gpus: 1
test: true
milestone: method_runs
notes: "Retry QURA with AdaRound alpha variables, warmup rounding regularization, 100 layer steps, 80 trigger steps, and lambda_B=1.0 after direct clipped rounding still collapsed clean accuracy."
