# This iteration writes files only — do NOT submit compute jobs.
# Once you set action to 'done', STOP immediately.
# Do NOT sleep, poll, or wait. The system reads this file
# only after your turn ends.
action: done
gpus: 0
test: false
milestone: majority
notes: "Improvements: fixed inferable held-out split, added baseline-adjusted ASR gain metric, completed metric descriptions. Notes: see DESIGN_NOTES.md; no evaluator jobs were run."
