# IMPORTANT: Once you set action to 'submit' or 'done', STOP immediately.
# Do NOT sleep, poll, or wait for jobs. The system reads this file
# only after your turn ends. Sleeping blocks the entire pipeline.
action: done
milestone: secondary_claims
notes: "Reproduced the core 4-bit claim, extended it to the 8-bit setting, and reproduced the weight-selection ablation showing that QURA preserves the attack/utility tradeoff while random or single-objective variants fail one side of the claim."
