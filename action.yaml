# IMPORTANT: Once you set action to 'submit' or 'done', STOP immediately.
# Do NOT sleep, poll, or wait for jobs. The system reads this file
# only after your turn ends. Sleeping blocks the entire pipeline.
action: submit
gpus: 1
test: true
milestone: none
notes: "Retry GPU smoke test after removing functional_call state mutation risk, switching to asymmetric per-channel weight quantization, and requiring full/std/QURA artifacts for non-empty scores."
