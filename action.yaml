# IMPORTANT: Once you set action to 'submit' or 'done', STOP immediately.
# Do NOT sleep, poll, or wait for jobs. The system reads this file
# only after your turn ends. Sleeping blocks the entire pipeline.
action: submit
gpus: 1
test: false
milestone: method_runs
notes: "Major QURA rewrite: fixed backdoor input caching (cache fp outputs for triggered inputs), layer-local L_A (compare to cached fp output at next layer), P(w)=|g_bd|/|g_acc| formula, bias handling. Using nvidia/cuda:12.4.0-runtime-ubuntu22.04 base. CIFAR-10 data pre-linked."