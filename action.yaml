# The system reads this file after the turn ends.
action: submit
gpus: 1
test: false
notes: "Retry train/test split evaluators after fixing train split list-index bug from job cceef978-26c; one missing ablation artifact will be reported as null."
