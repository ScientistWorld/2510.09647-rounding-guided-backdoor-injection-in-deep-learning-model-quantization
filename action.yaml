# IMPORTANT: Once you set action to 'submit' or 'done', STOP immediately.
# Do NOT sleep, poll, or wait for jobs. The system reads this file
# only after your turn ends. Sleeping blocks the entire pipeline.
action: submit
gpus: 1
test: true
milestone: method_runs
notes: "Retry QURA with 30 layer-optimization steps, optimized trigger steps 40, lambda_B=0.1, and non-clamped selected roundings to recover clean accuracy after the prior method_runs collapse."
