# IMPORTANT: Once you set action to 'submit' or 'done', STOP immediately.
# Do NOT sleep, poll, or wait for jobs. The system reads this file
# only after your turn ends. Sleeping blocks the entire pipeline.
action: submit
gpus: 1
test: true
milestone: secondary_claims
notes: "Audit restored secondary_claims and fixed baseline/reproduce workflow. Run trigger-generation ablation comparing Algorithm 1 optimized trigger against fixed white-trigger QURA on ResNet-18/CIFAR-10 4-bit."
